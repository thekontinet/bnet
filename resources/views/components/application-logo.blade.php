<h4 class="font-bold dark:text-slate-200 text-2xl flex items-center">
    <span class="flex items-center justify-center size-8 mr-1 rounded bg-blue-600 border border-blue-700 dark:text-slate-200">{{substr(config('app.name'), 0, 1)}}</span>
    <span>{{substr(config('app.name'), 1)}}</span>
</h4>
